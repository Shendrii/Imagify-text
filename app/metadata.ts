import type { Metadata } from "next";

export const defaultMetadata: Metadata = {
  title: "ImagifyText Sky",
  description: "Generate images from text and vice versa",
  // Add more metadata fields as needed
};
